<?xml version="1.0" encoding="utf-8"?>
<!-- F6 · app-lock icon seat: the gate rests on a dark elevated disc with a
     whisper-thin brass_12 ring — presence without alarm on the lockout
     screen, which stays ink-dark in both themes by design. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/bg_dark_elevated" />
    <stroke android:width="1dp" android:color="@color/brass_12" />
    <size android:width="80dp" android:height="80dp" />
</shape>
