<?xml version="1.0" encoding="utf-8"?>
<!-- f9 unified button family — GHOST: transparent fill, 1dp theme stroke
     (?attr/strokeColor), 16dp corners, brand-teal ripple at 20% alpha
     (visible on both light and dark surfaces). -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#332F7A6B">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#00000000" />
            <corners android:radius="16dp" />
            <stroke android:width="1dp" android:color="?attr/strokeColor" />
        </shape>
    </item>
</ripple>
