<?xml version="1.0" encoding="utf-8"?>
<!-- f9 unified button family — PRIMARY: theme-accent fill (teal in light,
     mint in dark), 16dp corners on the unified radius scale, parchment ripple
     at 20% alpha. Bounded by an explicit mask. Use for the one main CTA. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#33F2EFE9">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/accentColor" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</ripple>
